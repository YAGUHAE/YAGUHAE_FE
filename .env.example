# API 서버 origin — 서버 컴포넌트·서버 액션이 직접 부릅니다. `/api/v1`은 붙이지 않습니다.
# 브라우저는 이 값을 모릅니다(동일 출처 `/api/v1/*`만 봄).
API_ORIGIN=http://localhost:4000

# 1이면 `src/mocks/handlers.ts`에 핸들러가 있는 경로를 목으로 응답합니다.
# 핸들러가 없는 경로(BE가 이미 낸 엔드포인트)는 API_ORIGIN으로 나갑니다.
API_MOCK=1
